name: flutter_controls_auth
description: "A new Flutter package project."
version: 0.3.2
homepage:

environment:
  sdk: ^3.9.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

    # Needed for KeyCloak authentication support.

  openid_client: ^0.4.8
  url_launcher: ^6.2.1
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter: